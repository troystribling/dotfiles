"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const any_vega_1 = require("@nteract/any-vega");
const mime_1 = require("./mime");
/** Call the external library to do the embedding. */
async function embed(anchor, mediaType, spec, options = {}) {
    const version = mime_1.MEDIA_TYPES[mediaType];
    const defaults = {
        actions: false,
        mode: version.kind,
    };
    const embedThisVega = await any_vega_1.embed(version);
    return embedThisVega(anchor, JSON.parse(spec), Object.assign(Object.assign({}, options), defaults));
}
exports.embed = embed;
