import * as Immutable from "immutable";
import { AnyAction } from "redux";
export interface MessagesRecordProps {
    messageQueue: Immutable.List<AnyAction>;
}
export declare type MessagesRecord = Immutable.RecordOf<MessagesRecordProps>;
export declare const makeMessagesRecord: Immutable.Record.Factory<MessagesRecordProps>;
